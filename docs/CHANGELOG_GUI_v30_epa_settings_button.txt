[GUI v30] EPA 快捷按钮收敛为设置弹窗

- EPA 模块窗口顶部快捷区不再展开所有命令按钮，仅保留“EPA 设置”。
- 新增 EPA 设置弹窗：编队模式、调度方式、满级策略集中配置。
- 保留原有后端命令不变：-team / -single / -full / -equal / -keepmax / -stopmax / -r / -q / -a。
- 新增“应用选择”和“应用并运行”，通过 GUI 本地短序列发送，不依赖 UI 文案判断状态。
- 目标：减少误触、减少菜单冗余，保持 v19-lite 优化阶段的小步收敛。
